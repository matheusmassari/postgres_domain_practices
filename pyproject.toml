[project]
name = "postgres-practices-datasets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.2",
    "psycopg[binary]>=3.3.4",
]
